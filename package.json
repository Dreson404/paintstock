{
  "name": "paintstock",
  "version": "2.0.0",
  "description": "Native Android paint-stock manager with AI label scanning. Offline, per-device stock.",
  "private": true,
  "scripts": {
    "build": "node scripts/copy-web.js",
    "sync": "npm run build && npx cap sync android",
    "test": "node test_smoke.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/camera": "^6.1.0",
    "@capacitor/cli": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/preferences": "^6.0.2"
  },
  "devDependencies": {
    "jsdom": "^24.1.0"
  }
}
